syntax = "proto3";

package restaurant;
option go_package = "project/api/proto/restaurantpb";

message SetOrderReadyRequest {
    string order_id = 1;
}

message SetOrderReadyResponse {
    bool ok = 1;
}

service RestaurantService {
    rpc SetOrderReady(SetOrderReadyRequest) returns (SetOrderReadyResponse);
}